Added a filterable device list to the Backup History Diff tool showing each device's most recent backup, with retention via new "Backup Version Retention" fields on Golden Config Setting and a "Clean Up Backup Version Table" job, plus a bulk delete for choosing individual version records. A version is kept if it is within the retention window or among the most recent N. Retention and bulk delete remove index records only; configurations in Git are never modified.
